# pnpm 10 does not run a dependency's install scripts unless it is listed
# here. better-sqlite3 is a native addon backing the local chat store: skip
# its build and `pnpm install` still succeeds, then the app fails on the
# first write with a missing bindings file.
#
# Generated by the studio export from the monorepo's own allowlist. Edits
# here are overwritten on the next release.
onlyBuiltDependencies:
  - better-sqlite3
